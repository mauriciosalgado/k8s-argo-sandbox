apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: edc-appset
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: ${REPO_URL}
        revision: main
        files:
          - path: app-of-apps/apps/edc/cluster-dev/*.yaml

  template:
    metadata:
      name: edc-appset-helm-{{path.filename}}
    spec:
      project: default

      sources:
        - repoURL: https://charts.bitnami.com/bitnami
          chart: nginx
          targetRevision: 22.3.8
          helm:
            releaseName: "{{path.filename}}"
            valueFiles:
              - $path/app-of-apps/apps/edc/{{path.basename}}/{{path.filename}}
        - repoURL: ${REPO_URL}
          targetRevision: main
          ref: path

      destination:
        server: https://kubernetes.default.svc
        namespace: "{{path.basename}}"

      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
